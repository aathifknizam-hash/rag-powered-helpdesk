# Production requirements (compatibility alias)
# This file keeps the same production setup as requirements_prod.txt.

-r requirements.txt

# Production server / deployment
gunicorn==21.2.0
whitenoise==6.6.0
uvicorn==0.25.0

# Redis / background jobs
redis==5.0.1
celery==5.4.0
django-celery-beat==2.5.0
channels-redis==4.1.0
django-redis==5.4.0

# ASGI / websocket support
daphne==4.1.1

# Monitoring and security
sentry-sdk==1.41.0
cryptography==41.0.7
pycryptodome==3.19.0
pyotp==2.9.0
